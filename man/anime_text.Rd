% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/text.R
\name{anime_text}
\alias{anime_text}
\title{Specify discrete text keyframes for an element}
\usage{
anime_text(values)
}
\arguments{
\item{values}{An atomic vector of the successive text values. Non-character
vectors are coerced with \code{\link[=as.character]{as.character()}}, so pre-format numbers (e.g. with
\code{\link[=format]{format()}} or \code{\link[=formatC]{formatC()}}) if you need thousands separators or fixed
notation.}
}
\value{
An \code{anime_text} object.
}
\description{
Constructs a text-swap specification for use in the \code{props} argument of
\code{\link[=anime_add]{anime_add()}}. Unlike a numeric tween, a text prop does not interpolate:
the values are spread evenly across the segment's duration and the target
element's \code{textContent} is swapped to the value for the current position as
the timeline plays or is scrubbed. Useful for animated titles, counters,
and tickers.
}
\details{
The segment's \code{selector} picks the text element(s) to update, so the prop
name you file this under is only a label; use something descriptive such as
\code{text} or \code{label}.
}
\examples{
# A counter that steps through four values across the segment
anime_add(
  anime_timeline(),
  selector = anime_target_id("counter"),
  props = list(text = anime_text(c("0", "25", "50", "100")))
)

# Compose a text swap with an ordinary tween on the same segment
anime_add(
  anime_timeline(),
  selector = anime_target_id("title"),
  props = list(
    opacity = anime_keyframes(0, 1),
    label = anime_text(c("Loading", "Ready"))
  )
)

}
